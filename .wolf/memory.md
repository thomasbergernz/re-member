# Memory

> Chronological action log for the re-member blueprint.
> Hooks and AI append to this file automatically.
| 21:48 | De-brand commit c0c9995: brand+infra neutralised, risky artifacts removed (.har+probes), org identity moved to env vars, tests 197/197 green | many | ok | ~6100 |
| 22:05 | Schema-driven form system Phase A foundation: types.ts + validators.ts + runtime.ts + tiers.ts + FieldRenderer.astro + Step.astro + InlineRenderer.astro + form-client.ts (+tests). 233/233 tests pass. EMAIL_RE unified to header-injection-safe regex (closed latent bug in apply.ts:35). docs/CUSTOMIZE.md section 7 rewritten to three sub-sections (schema-driven / engineers-only / not-yet-migrated). | src/lib/forms/** docs/CUSTOMIZE.md | ok | ~3200 |
| 09:15 | Phase B Associate renewal pilot landed (B1+B2). New: renewAssociate schema (TS+JSON), renewAssociate.test.ts (8 tests), integer validator kind, dynamic [tier].astro page, dynamic checkout/[tier].ts handler, checkout/[tier].test.ts (9 tests). Deleted: associate.astro, checkout-am.ts, checkout-am.test.ts. 244/244 tests pass. | src/lib/forms/schemas/renewAssociate.* src/pages/renew/[tier].astro src/pages/api/renew/checkout/[tier].ts | ok | ~2800 |
| 13:09 | Phase F validate() honor-required fix landed. runtime.ts validate() now checks `field.required && isBlank(value)` BEFORE the validators loop; suppressed when an explicit blank-rejecting validator (required/conditional/ynRadio/jsonArray) is already in validators[]. Added `requiredMessage?: string` to BaseField. 5 new runtime.test.ts cases (3 plan + 2 extra: requiredMessage override + explicit-wins). Tests: 281/281 pass. Pre-existing ts(2571) in pd-log.ts:63 unchanged. | src/lib/forms/types.ts src/lib/forms/runtime.ts src/lib/forms/runtime.test.ts | ok | ~1900 |
| 13:23 | Phase G Pro renewal wiring landed. [tier].astro TIER_SCHEMAS gains `professional` (renewProSchema + renewProContent); module script serialises schema to JSON island, mounts via form-client.ts. [tier].ts now extracts phone + pdEntries from values, passes through to appendRenewal + Stripe metadata + cancel URL. coercePdEntries() filters malformed entries (defence in depth — Phase F's implicit required doesn't reach nested repeatable children). Deleted: pro.astro + checkout-pm.ts + checkout-pm.test.ts. Tests: 272/272 pass (was 281; -11 deleted checkout-pm.test.ts + 2 new pro cases). Typecheck: still 1 pre-existing error in pd-log.ts:63. | src/pages/renew/[tier].astro src/pages/api/renew/checkout/[tier].ts src/pages/api/renew/checkout/[tier].test.ts | ok | ~2400 |
| 13:52 | Phase H PD log wiring landed. pdLog schema reshaped: `entries` is now a `repeatable` (was 4 flat fields). pd-log.astro renders via Step + FieldRenderer; module script imports mount + hydrateFromResponse from form-client.ts (`@ts-nocheck` for DOM-glue code). pd-log.ts handler synthesises per-entry schema from itemFields, validates each entry via validate() (closes pre-existing ts(2571) at old line 63). pdLog.test.ts rewritten: 5 cases (id, structure, validate-empty, validate-array, minRows, columnMap). Tests: 275/275 pass (+3 net from rewritten schema tests). Typecheck: 0 errors. | src/lib/forms/schemas/pdLog.ts src/lib/forms/schemas/pdLog.content.json src/lib/forms/schemas/pdLog.test.ts src/pages/renew/pd-log.astro src/pages/api/renew/pd-log.ts | ok | ~2900 |
| 14:00 | Phase I Associate apply wiring landed + toRow bug fix (bug-002). apply.astro: 326→127 lines, Step + FieldRenderer loop; module script imports mount from form-client.ts; URL-param prefill + today-default applicationDate via initialValues. create-checkout-session.ts: replaced manual EMAIL_RE + 13 required-field checks with `validateTier("associate", body)` for the associate-apply branch; non-apply paths (plan="professional" etc.) untouched. Named-arg adapter pattern for appendAssociateApplication (same as Phase B renewals). associateApply.test.ts: +2 cases (visibleWhen excludes listingDetails from values + toRow row.M undefined; all 13 columns C-O via toRow when listOnPage="yes"). Bug-002 fix: toRow now early-continues when field.visibleWhen && !field.visibleWhen(values). Tests: 277/277 pass. Typecheck: 0 errors. | src/pages/apply.astro src/pages/api/create-checkout-session.ts src/lib/forms/schemas/associateApply.test.ts src/lib/forms/runtime.ts | ok | ~3400 |
| 14:13 | Phase J1 Pro apply render-only landed. apply.astro: 1692→1295 lines (-397). Sections 1-7 (steps about → declarations) now Step + FieldRenderer loop; section IDs preserved for the existing showStep() nav; step 8 (uploads) stays inline for J3. FieldRenderer changed: `inputId = inputName` (no `f-` prefix, no hyphen transform — top-level field IDs now equal their name attribute, matching existing step-1 hydration). Deleted inline JS: buildCompetencyGrid, addCourseRow, addExperienceRow + their addEventListener calls (replaced by form-client attachRepeatable for the schema-driven repeatables + grid). Stubbed saveFormData's obsolete references to deleted state (courses/experiences/competencyStates) — J2 will rewrite as FormData(form) reads. professionalApply.test.ts: +1 case (8 steps in declaration order). Tests: 278/278 pass. Typecheck: 0 errors. Renders correctly; autosave/submit for steps 2-6 will be properly FormData-wired in J2. | src/lib/forms/render/FieldRenderer.astro src/pages/professional/apply.astro src/lib/forms/schemas/professionalApply.test.ts | ok | ~4500 |
| 14:20 | Phase L ELDAA-neutrality landed. Removed hardcoded `COMPETENCY_IDS` + `FURTHER_REQUIREMENT_IDS` TS constants (21+8 ELDAA-specific strings like "effectiveCommunication", "advocacyEmpowerment"). Now derived at schema-build time: `Object.keys(content.steps.competencies.fields.coreCompetencies.options)` (and similar for furtherRequirements). TS schema is now pattern-only; per-org content (the actual competency names + further-requirement questions) lives in the JSON content file. Fork-er edits JSON to customize their own competency framework without touching TS. Re-exported COMPETENCY_IDS for backward compat with tests. FURTHER_REQUIREMENT_IDS kept local (not referenced externally). Tests: 278/278 pass. Typecheck: 0 errors. | src/lib/forms/schemas/professionalApply.ts | ok | ~4800 |
| 14:29 | Phase J2 Pro apply client runtime landed. apply.astro: sections 1-7 now wrapped in `<form id="professional-apply-form">` so FormData(form) works. `saveFormData` rewritten as `buildPayload(fd)` + FormData(form) read — flattened dotted names (referees.referee1Name → referee1Name), serialised repeatables (qualifications/experience) as JSON arrays, grid furtherRequirements as JSON object, grid coreCompetencies as JSON array in column order (read from hidden `<script id="competency-order">`), declaration checkboxes as "TRUE"/"". Module script (Astro `<script>`) imports `mount` + `attachAutosaveQueue` from form-client; calls mount() + wires debounced (800ms) autosave with custom serialize + onSuccess callback that updates window.__applicantId__/__token__. Helpers exposed on window (`__buildPayload__`, `__serializeRepeatable__`, `__flushAutosave__`) so the existing inline script's queueFormSave + Continue/Back click handlers can share the same payload shape. Bug fix in `assertOptionValuesExist`: was only checking visibleWhen on select/radio fields with their OWN options — now collects every option value across the schema then checks every visibleWhen predicate against the global set. Tests: 282/282 pass (+4 net: form-client.test.ts covers assertOptionValuesExist for no-violations / valid / invalid predicate / group descent). Typecheck: 0 errors. DOM-glue tests for attachAutosaveQueue/attachRepeatable/attachVisibleWhen/mount deferred to a follow-up that wires jsdom/happy-dom. | src/pages/professional/apply.astro src/lib/forms/render/form-client.ts src/lib/forms/render/form-client.test.ts | ok | ~5400 |
| 14:36 | Phase J3 Pro apply upload + complete landed. Deleted hardcoded `REQUIRED_DOCS` + `OPTIONAL_DOCS` TS arrays. Doc types now derived from `professionalApplySchema.uploads.docTypes` via `ALL_DOC_TYPES` constant; per-docType descriptions (with HTML allowed) stay inline in `DOC_DESCRIPTIONS` map keyed by id. `renderDocuments()` iterates `ALL_DOC_TYPES`; payment-button enable check uses `REQUIRED_DOC_TYPES.length` instead of hardcoded 6. Upload button state + per-file XHR binary upload logic preserved (Phase A skeleton `attachUploadLock` not used — current uploadsInFlight queue across all buttons is more correct than per-button lock for this multi-file flow). Tests: 282/282 pass. Typecheck: 0 errors. | src/pages/professional/apply.astro | ok | ~5700 |

| 14:43 | Phase K Tier abstraction defence-in-depth landed. Widened `"pm"|"am"` literal unions to `string` in renewal-sheet.ts (RenewalInput.tier, RenewalRow.tier) + email-sender.ts (sendRenewalAdminNotification tier param). getRenewalById reader no longer casts: `tier: string = validStorageValues.has(rawTier) ? rawTier : "pm"`. stripe-products.ts: LookupKey widened from `"pm_renewal_nzd"|"am_renewal_nzd"` to template literal type `${string}_renewal_nzd`; `getPriceEnvVar` derives the env var from listTiers() by matching storageValue prefix (uses `t.priceEnvVar`). email-sender.ts: new `tierLabelFor(storageValue)` helper looks up label from TIERS — replaces the `tier === "pm" ? "Professional Member" : "Associate Member"` ternary. create-checkout-session.ts: Stripe line_item product name resolved via `getTier(plan).label` instead of `"Associate Membership" / "Professional Membership"` literal. [tier].ts: dropped the `as "pm"|"am"` cast — `tier` is now `string` directly. Tests: 282/282 pass. Typecheck: 0 errors. Skipped (out of scope): stripe-webhook.ts `plan === "professional" / plan === "associate"` deep business-logic branches (242/299/324/334) — would require restructuring the entire post-payment side-effect flow; presentation-only success.astro plan check (45) stays. | src/lib/renewal-sheet.ts src/lib/email-sender.ts src/lib/stripe-products.ts src/pages/api/renew/checkout/[tier].ts src/pages/api/create-checkout-session.ts | ok | ~6500 |

| 15:47 | Phase M tier rename landed. professional→advanced, associate→basic. Storage values pm→adv, am→basic. Schema files + page directories renamed. Functions/variables/test fixtures all updated. Env vars STRIPE_PRICE_* updated. MembershipPlan type derived from keyof typeof TIERS (auto-extends for N tiers). Stripe lookupKey widened to `${string}_renewal_nzd` template literal. Data-driven dispatch in stripe-checkout.ts (getPriceForPlan/getPlanDisplayName), email-sender.ts (tierLabelFor), health.ts (renewal_prices map iterated from listTiers()), upload-sheet.ts (DEFAULT_SHEET_NAME from getTier). Tests: 282/282 pass. Typecheck: 0 errors. Pushed: commit 80a7218 on feat/phase-fghi-schema-driven-migration. PR #8 still open; will need separate PR for rename once the F→K+L one merges (or force-push if same branch is fine). | src/ (~80 files) docs/CUSTOMIZE.md | ok | ~9000 |
| 22:21 | designqc: captured 6 screenshots (204KB, ~15000 tok) | / | ready for eval | ~0 |
| 22:23 | FieldRenderer CSS gap closed. global.css gained @layer components block defining .field, .field-label, .required-mark, .form-step (white card + 2-col CSS grid), .step-title, .field input/textarea/select (rounded-xl large-padding matching professional/apply.astro registration), .radio-group, .checkbox-label, .field-group, .repeatable, .field-grid, .field-help, .field-error. Textarea + radio + checkbox + grid + group + repeatable rows span both columns via :has() selectors. tests 283/283 pass, typecheck 0 errors. designqc confirms /apply now matches ELDAA-style (clean 2-col paired fields, full-width textareas/radios, "Continue to payment" CTA visible).
| 22:28 | designqc: captured 6 screenshots (202KB, ~15000 tok) | / | ready for eval | ~0 |
| 09:08 | Adopted spec-driven-dev (papaoloba/spec-based-claude-code pattern). 10 slash commands copied verbatim to .claude/commands/spec/*; 3 templates to spec/_templates/*. 16 spec dirs backfilled (000 index + 001-006 member-facing + 007-011 system + 012-015 cross-cutting). Each spec = requirements.md + design.md + tasks.md; no approval dotfiles (backfill mode). CLAUDE.md stripped from 407→168 lines (removed duplicated domain-reference section; replaced with pointer to spec/000-platform-overview). REQ-ID convention: REQ-{SPEC-ID}-{NNN}, stable, referenced in commits + PRs. Tests 283/283, typecheck 0 errors. Branch: feat/spec-driven-dev. Commit 0c73b44. | .claude/commands/spec/* spec/_templates/* spec/000..015/ CLAUDE.md | ok | ~3500 |
| 18:49 | Task 1: readNotificationRules() sheet reader implemented in /Users/thomasb/eldaa. Added mockGet to test mock setup, two test cases (returns rows + filters empty emails; returns empty array), and function that reads A2:C from "Notification Rules" sheet, filters row[0] && row[1], maps to {event, recipient_email, enabled}. Tests: 15/15 pass. Commit 98d1840. Report written to .superpowers/sdd/task-1-report.md. | src/lib/google-sheets.ts src/lib/google-sheets.test.ts | ok | ~1400 |
| 09:30 | Ported eldaa's Sheet-driven Notification Rules into re-member. NEW src/lib/notification-rules.ts (NotificationEvent union: 3 wired + 4 reserved; getRecipientsForEvent(event, fallback?)). google-sheets.ts gained self-contained ensureNotificationRulesSheet (NOT shared ensureSheetWithHeaders — that helper's unconditional values.update reverts admin edits) + readNotificationRules (A2:C). Wired 3 webhook sites (advanced_renewal_received/advanced_payment_received/basic_payment_received) as fire-and-forget forEach. DIVERGENCE from eldaa: fallback param → returns [envEmail] on read-fail OR no-match (notifications never silently drop). Docs §6a in CUSTOMIZE.md. Tests 296/296, check 0 errors. | src/lib/notification-rules.ts(+test) src/lib/google-sheets.ts(+test) src/pages/api/stripe-webhook.ts(+test) docs/CUSTOMIZE.md | ok | ~5000 |
